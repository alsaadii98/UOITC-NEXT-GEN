import React from 'react';
import DotsIcon from '../assets/Icons/Dots.svg'

const Dots = (props) => {
    return <img className={props.className} src={DotsIcon} alt={'Dots'}/>

};

export default Dots;



